import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './containers/App';
import 'tachyons';

ReactDOM.render(
    <App />, // the robots prop is going to be the robots array from robots.js
    document.getElementById('root')
);


